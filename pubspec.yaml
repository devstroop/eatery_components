name: eatery_components
description: A new Flutter project.
version: 0.0.1
homepage:

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  uicons: ^1.0.1
  path: ^1.9.0
  http: ^1.2.1
  image_picker: ^1.1.2
  file_picker: ^8.0.6
  path_parsing: ^1.0.1
  flutter_svg: ^2.0.10+1
  eatery_db:
    path: "../eatery_db"
    # git:
      # url: 'https://github.com/devstroop/eatery_db.git'
      # ref: 'main'
  # (archived) eatery_services:
    # git:
      # url: 'https://github.com/devstroop/eatery_services.git'
      # ref: 'main'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/vectors/
    - assets/
